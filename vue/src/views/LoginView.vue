<template>
<div>
  <!-- This has no UI to redirect the user after a successful login to the destination page with the least amount of blinking -->
</div>
</template>

<script setup lang="ts">
// import { ref, watchEffect } from 'vue';
// import { storeToRefs } from 'pinia'
// import { useMainStore } from '@/store';
// import { useAuth0 } from '@auth0/auth0-vue';

// const store = useMainStore();
// const { currentTopic } = storeToRefs(store);
// const { isAuthenticated, isLoading } = useAuth0();

// /// redirect to subscription page if the user is authenticated
// watchEffect(() => {
//   console.log(`LoginView: Auth status: ${isLoading.value}/${isAuthenticated.value}`);
  
//   if (isAuthenticated.value) {
//     console.log("User is authenticated");
//     // router.replace({ name: PageIDs.SUBSCRIPTION });
//   }
// });

</script>
